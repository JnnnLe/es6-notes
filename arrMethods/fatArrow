//fat Arrow fn uses lexical scoping

//Object literals
  //when key and value have the same name, they only need to be written once.
    // {example: example,
        name: 'Jenn'} === 
       {example, name: 'Jenn'}

  //when there is a method, omit the function keyword and '='
    // fn ... {
      example = function() {
        ....execute
      }
    } ===
    //fn ... {
      example() {
        ....execute
      }
    }
