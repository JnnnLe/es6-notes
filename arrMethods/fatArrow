//fat Arrow fn uses lexical scoping

//Enhancing Object literals

  //Same key/value name:
    //when key and value have the same name, they only need to be written once.
      // {example: example,
          name: 'Jenn'} === 
        {example, name: 'Jenn'}

  //Methods in objects:
    //when there is a method, omit '`methodName`: function()'
      // fn ... {
        example : function() {
          ....execute
        }
      } ===
      //fn ... {
        example() {
          ....execute
        }
      }
