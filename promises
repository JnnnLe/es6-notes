//ES6 PROMISES

    //ES6 makes promises uniform for all browsers and no need for a third party library

    //3 states: unresolved, resolved(then), rejected(catch)

    // code examples:

    //snippet
    promise = new Promise((resolve, reject) => { resolve() }, 2000);

    promise
      .then(() => console.log('Completed'))
      .then(() => console.log('X2'))
      .catch(() => console.log('exit'))